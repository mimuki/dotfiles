#!/bin/sh 
# --- [ Essential Software ] ------------------------------------------------------- #
# (if a program has additional install stpes, consider putting it's install
# further down)
sudo apt-get update
sudo apt install keynav tlp git gh kdeconnect vim mpv scrot xclip xterm tmux
