--------------------------------------------------------------------------------
--                                  vars.lua                                  --
--------------------------------------------------------------------------------
local xresources = require("beautiful.xresources")
local dpi = xresources.apply_dpi 
----- [ General ] --------------------------------------------------------------
modkey = "Mod4" -- GUI/Super/Meta/Windows etc
terminal = "kitty"
editor = os.getenv("EDITOR") or "vim"
editor_cmd = terminal .. " -e " .. editor

homeRow = {"a", "r", "s", "t", "g", "m", "n", "e", "i", "o", "semicolon" }
totalTags = 5 -- Change to increase or decrease number of tags generated

-- Enabled layouts
awful.layout.layouts = {
  awful.layout.suit.tile,
  awful.layout.suit.tile.top,
  awful.layout.suit.max.fullscreen,
}
----- [ Widgets ] --------------------------------------------------------------
-- This is generated automatically by Chezmoi
{{- if eq .chezmoi.hostname "DESKRATS-2020MNT21" }}
-- This machine should be DESKRATS-2020MNT21
frontTimeout = 300
{{- else if eq .chezmoi.hostname "LAPRATS-2023MNT21" }}
-- This machine should be LAPRATS-2023MNT21
frontTimeout = 5
{{ else }}
-- This machine wasn't recognized by Chezmoi
frontTimeout = 600
{{ end }}
